<ui:FluentWindow x:Class="AorusControl.App.ProgramPickerWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Programm suchen" Icon="Assets/app.ico" Width="560" Height="560"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Programm suchen" ShowMaximize="False" />

        <StackPanel Grid.Row="1" Margin="20,8,20,0">
            <ui:TextBox x:Name="SearchBox" PlaceholderText="Name oder Pfad …" FontSize="14"
                        TextChanged="OnSearchChanged" />
            <TextBlock Text="{Binding Summary}" Style="{StaticResource Hint}" />
        </StackPanel>

        <!-- Virtualisiert, weil hier zweihundert Einträge stehen und jeder eine Zeile mit
             zwei Textblöcken ist. -->
        <ListBox Grid.Row="2" Margin="20,12,20,0" x:Name="Results"
                 ItemsSource="{Binding Results}" SelectedItem="{Binding Selected, Mode=TwoWay}"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                 MouseDoubleClick="OnResultDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="2,4">
                        <TextBlock Text="{Binding Name}" FontSize="13.5" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding Identity}" Style="{StaticResource Hint}" Margin="0,2,0,0"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,16,20,20">
            <ui:Button Style="{StaticResource SecondaryButton}" Content="Datei auswählen …" Click="OnBrowseClick" Margin="0,0,10,0"
                       ToolTip="Für Programme ohne Startmenü-Eintrag, etwa Spiele aus einer Bibliothek." />
            <ui:Button Style="{StaticResource SecondaryButton}" Content="Abbrechen" Click="OnCancelClick" Margin="0,0,10,0" />
            <ui:Button Style="{StaticResource PrimaryButton}" Content="Hinzufügen" Click="OnAcceptClick"
                       IsEnabled="{Binding HasSelection}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
